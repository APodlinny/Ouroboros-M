#include "SchemeValidator.h"

using namespace Ouroboros::Scheme;

void SchemeValidator::Validate(const Scheme& scheme)
{
	ValidateBindings(scheme);
	ValidateNodeDescriptions(scheme);
}

void SchemeValidator::ValidateBindings(const Scheme& scheme)
{
	
}

void SchemeValidator::ValidateNodeDescriptions(const Scheme& scheme)
{

}